name: Build ZMK firmware
on: [workflow_dispatch]

#jobs:
#  build:
#    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
jobs:
  build:
    uses: wittyrascal/miryoku_zmk/.github/workflows/build.yml@master
